version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build-and-export
  artifacts:
    baseDirectory: out   # matches distDir in next.config.ts
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
